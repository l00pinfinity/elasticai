import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.boitdroid.elasticai',
  appName: 'elasticai',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
